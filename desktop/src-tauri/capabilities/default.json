{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The signed Synchri desktop window owns its bundled local engine.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default"
  ]
}
